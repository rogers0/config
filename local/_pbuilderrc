# this is your configuration file for pbuilder.
# read pbuilderrc.5 document for notes on specific options.
SOURCE_ONLY_CHANGES=yes
PDEBUILD_PBUILDER=cowbuilder
MIRRORSITE=https://deb.debian.org/debian
BASEDIR=~rosh/working
CCACHEDIR=/var/cache/pbuilder/ccache
BINDMOUNTS=${BASEDIR}/build-area
BUILDRESULT=$BINDMOUNTS
HOOKDIR=${BASEDIR}/hook
#if [ -z "${DIST}" ] && [ -r "debian/changelog" ]; then
#    DIST=$(dpkg-parsechangelog | awk '/^Distribution: / {print $2}')
#fi
#OTHERMIRROR="$OTHERMIRROR | deb ${MIRRORSITE} ${DIST} main"
#OTHERMIRROR="$OTHERMIRROR | deb [trusted=yes] file://${BINDMOUNTS} ./"
#OTHERMIRROR="$OTHERMIRROR | deb http://incoming.debian.org/debian-buildd buildd-${DIST} main"
EXTRAPACKAGES="apt-utils apt-transport-https ca-certificates debhelper eatmydata lintian ccache autodep8 autopkgtest"
#PBUILDERSATISFYDEPENDSCMD="/usr/lib/pbuilder/pbuilder-satisfydepends-apt"		# required for cross-build
EATMYDATA=yes
#USENETWORK=yes

# export for D05deps script
export BINDMOUNTS
